# For local development (proxied via package.json "proxy" field)
# No need to set this if using the proxy

# For production deployment (Vercel / Netlify)
REACT_APP_API_URL=https://social-app-qj5m.onrender.com
